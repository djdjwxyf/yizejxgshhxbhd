/* ============================================================
 * 相册 · 配置
 * ------------------------------------------------------------
 * 照片存在仓库的一个独立分支里（不是 main），
 * 这样「hexo d 重新部署网站」永远不会把照片冲掉。
 *
 * 要改的地方基本只有 groups（分组）。
 * ============================================================ */

window.PHOTO_CONFIG = {
  owner: 'djdjwxyf',
  repo: 'yizejxgshhxbhd',

  /* 照片存放的分支。⚠️ 不要改成 main —— 那是网站部署分支，
     每次 hexo d 都会强制覆盖，照片会没。 */
  branch: 'photos',

  /* 分支里存放照片的目录 */
  dir: 'img/album',

  /* 分组：key 会作为子目录名（img/album/<key>/），
     label 是页面上显示的名字。想加分组就往这里加一条。 */
  groups: [
    { key: 'memories', label: '我们的回忆' },
    { key: 'travel', label: '旅行日记' }
  ],

  /* 上传时的图片压缩参数：最长边 1600px、JPEG 质量 0.82。
     手机原图 3–8MB，压完通常 200–500KB，够清晰又不占地方。 */
  maxSide: 1600,
  quality: 0.82
};
